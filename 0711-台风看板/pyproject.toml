[project]
name = "typhoon-dashboard"
version = "0.1.0"
description = "台风巴威实时监测大屏系统"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "pydantic>=2.8",
]
